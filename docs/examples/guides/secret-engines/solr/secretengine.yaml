apiVersion: engine.kubevault.com/v1alpha1
kind: SecretEngine
metadata:
  name: solr-engine
  namespace: demo
spec:
  vaultRef:
    name: vault
  solr:
    databaseRef:
      name: solr
      namespace: demo
    pluginName: solr-database-plugin
    allowedRoles:
      - "*"
    insecure: false
