apiVersion: secrets-store.csi.x-k8s.io/v1alpha1
kind: SecretProviderClass
metadata:
  name: vault-db-provider
  namespace: demo
spec:
  provider: vault
  parameters:
    vaultAddress: "http://vault.demo:8200"
    roleName: "k8s.-.demo.solr-reader-role"
    objects: |
      - objectName: "solr-creds-username"
        secretPath: "your-database-path/creds/k8s.-.demo.solr-superuser-role"
        secretKey: "username"
      - objectName: "solr-creds-password"
        secretPath: "your-database-path/creds/k8s.-.demo.solr-superuser-role"
        secretKey: "password"
