apiVersion: secrets-store.csi.x-k8s.io/v1alpha1
kind: SecretProviderClass
metadata:
  name: vault-db-provider
  namespace: demo
spec:
  provider: vault
  parameters:
    vaultAddress: "http://vault.demo:8200"
    roleName: "k8s.-.demo.zookeeper-app-role"
    objects: |
      - objectName: "zookeeper-creds-username"
        secretPath: "your-database-path/static-creds/k8s.-.demo.zookeeper-app-role"
        secretKey: "username"
      - objectName: "zookeeper-creds-password"
        secretPath: "your-database-path/static-creds/k8s.-.demo.zookeeper-app-role"
        secretKey: "password"
