apiVersion: engine.kubevault.com/v1alpha1
kind: SecretEngine
metadata:
  name: rabbitmq-engine
  namespace: demo
spec:
  vaultRef:
    name: vault
  rabbitmq:
    databaseRef:
      name: rabbitmq
      namespace: demo
    pluginName: rabbitmq-database-plugin
    allowedRoles:
      - "*"
