apiVersion: secrets-store.csi.x-k8s.io/v1alpha1
kind: SecretProviderClass
metadata:
  name: vault-db-provider
  namespace: demo
spec:
  provider: vault
  parameters:
    vaultAddress: "http://vault.demo:8200"
    roleName: "k8s.-.demo.neo4j-reader-role"
    objects: |
      - objectName: "neo4j-creds-username"
        secretPath: "your-database-path/creds/k8s.-.demo.neo4j-superuser-role"
        secretKey: "username"
      - objectName: "neo4j-creds-password"
        secretPath: "your-database-path/creds/k8s.-.demo.neo4j-superuser-role"
        secretKey: "password"
